CMAKE_DEPENDENT_OPTION(NEKTAR_SOLVER_REVIEWSOLUTION
    "Build the review solution Solver." ON
    "NEKTAR_BUILD_SOLVERS" OFF)

IF (NOT NEKTAR_BUILD_SOLVERS)
    SET(NEKTAR_SOLVER_REVIEWSOLUTION OFF CACHE INTERNAL "")
ENDIF()

IF( NEKTAR_SOLVER_REVIEWSOLUTION )
    ADD_SOLVER_EXECUTABLE(ReviewSolution SOURCES 
        ./ReviewSolution.cpp
        ./EquationSystems/FileSolution.cpp)

    ADD_NEKTAR_TEST(ReviewSolution_3DH1D)
    ADD_NEKTAR_TEST(ReviewSolution_2D)

ENDIF( NEKTAR_SOLVER_REVIEWSOLUTION )
